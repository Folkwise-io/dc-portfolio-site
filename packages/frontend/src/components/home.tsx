import React from 'react';

export const Home = () => {
  return (
    <div>
      <div>This is the home page!</div>
      <button>Would you like to sign up?</button>
    </div>
  );
};

export default Home;
