{
  "name": "dc",
  "version": "1.0.0",
  "main": "index.js",
  "author": "Dev Collective",
  "license": "MIT",
  "private": true,
  "repository": "git@github.com:Mintbean/dc-portfolio-site.git",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "yarn workspaces run dev",
    "start-backend": "yarn --cwd packages/backend dev-nodemon",
    "start-frontend": "yarn --cwd packages/frontend start",
    "db-init": "yarn --cwd packages/backend db:init",
    "db-migrate": "yarn --cwd packages/backend db:migrate",
    "db-seed": "yarn --cwd packages/backend db:seed"
  },
  "dependencies": {
    "typescript": "^4.2.3"
  }
}
